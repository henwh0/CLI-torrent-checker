# CLI-torrent-checker
Prints a status box of qBittorent downloads to your CLI.

You may have to install the qbittorrent-api library with 'pip install qbittorrent-api'

input your own information in the dictionary value fields.
